'use strict'

var numero1 = 10;
var numero2 = 12;

// Llama la funcion y agregas los valores, asignandolos segun el orden de la función
calculadora(numero1, numero2);

// Esta es la función donde se suman 
function calculadora(valor1, valor2) {
    var resultado = valor1+valor2;
    return resultado;    
}